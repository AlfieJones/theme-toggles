import { BaseTogglePropsType, createDiv } from "../../utils";
import "../../../../dist/base/{{dashCase name}}.css";

export default (args: BaseTogglePropsType) =>
  createDiv({ ...args, name: "{{dashCase name}}.svg" });
