import { BaseTogglePropsType, createCheckbox } from "../../utils";
import "../../../../dist/checkbox/{{dashCase name}}.css";

export default (args: BaseTogglePropsType) =>
  createCheckbox({ ...args, name: "{{dashCase name}}.svg" });
