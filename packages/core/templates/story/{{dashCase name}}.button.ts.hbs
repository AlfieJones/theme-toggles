import { BaseTogglePropsType, createButton } from "../../utils";
import "../../../../dist/base/{{dashCase name}}.css";

export default (args: BaseTogglePropsType) =>
  createButton({ ...args, name: "{{dashCase name}}.svg" });
