name: "Pixeleye core"

on:
  push:
    # paths:
    #   - packages/core/**

jobs:
  pixeleye-core-deployment:
    runs-on: ubuntu-latest
    env:
      PUPPETEER_SKIP_DOWNLOAD: true
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
        with:
          version: 8
      - name: Use Node.js 20
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: "pnpm"
      - name: Install dependencies
        run: pnpm install
      - name: Capture stories with Pixeleye
        run: npx lerna run storybook --scope=@theme-toggles/core & pnpm pixeleye storybook -F @theme-toggles/core
