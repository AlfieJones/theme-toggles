---
"@theme-toggles/react": patch
---

Move shadcn to development dependencies so it is not installed by package consumers, and add React as a development dependency for local tooling while retaining it as a peer dependency for consumers.
