---
"@theme-toggles/react": patch
---

Publish compiled JavaScript and TypeScript declarations instead of raw TypeScript sources so the package works with Next.js Turbopack. Mark React components as client components for App Router compatibility.
